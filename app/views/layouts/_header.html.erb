<nav class="navbar navbar-fixed-top" style="background-color: #00ada9;">
    <div class="navbar-header">
        <div class="container">
            <% if logged_in? %>
            <nav>
             <%= link_to fa_icon("diamond")+" Daisuki Club Online Judge", main_menu_path, class: "navbar-brand" %>
             <%= link_to fa_icon("plus")+ " Create Question", new_question_path, class: "navbar-brand" %>
             <%= link_to fa_icon("file-text-o")+" Question List", questions_path, class: "navbar-brand" %>
             <ul class="nav navbar-nav pull-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle navbar-brand" data-toggle="dropdown"><%= current_user.account %><b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li><%= link_to fa_icon("address-card") + " Profile", current_user %></li>
                        <li class="divider"></li>
                        <li><%= link_to fa_icon("user") + " Follower #{current_user.followers.length}", follower_list_user_path(current_user) %></li>
                        <li class="divider"></li>
                        <li><%= link_to fa_icon("user-o") + " Following #{current_user.following.length}", following_list_user_path(current_user) %></li>
                        <li class="divider"></li>
                        <li><%= link_to fa_icon("sign-out") + " Log out", logout_path, method: "delete" %></li>
                    </ul>
                </li> 
            </ul>
        </nav>
        <% else %>
        <nav>
            <%= link_to fa_icon("diamond") +  "Daisuki Club Online Judge", root_path, class: "navbar-brand" %>
            <div class="pull-right">
                <%= link_to "Sign in", login_path, class: 'navbar-brand' %>
            </div>
        </nav>
        <% end %>
    </div>
</div>
</nav>
