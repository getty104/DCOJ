 <table class="table table-borderd"> 
	<tbody>
		<% @recent_contests.each do |contest| %>
		<tr>
			<td>
				<%= contest.start_time %>
				<br>
		
				<br>
				<%= link_to "title: #{contest.title}", contest %>
		</tr>
		<% end %>
	</tbody>
 </table>
