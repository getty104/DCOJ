<%= form_for :user, url: do_block_user_path(@user), remote: true do |f| %>
<div><%= hidden_field_tag :block_account, @user.account %></div>
  <%= f.submit "Block", class: "btn btn-danger btn-lg" %>
<% end %>