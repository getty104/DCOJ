<%= form_for :user, url: do_unblock_user_path(@user), remote: true do |f| %>
<div><%= hidden_field_tag :block_account, @user.account %></div>
  <%= f.submit "Unblock", class: "btn btn-default btn-lg col-xs-6 col-sm-6 col-md-1" %>
<% end %>